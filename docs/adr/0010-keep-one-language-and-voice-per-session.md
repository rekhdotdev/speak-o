# Keep one language and Voice per Reading Session

Speak-O resolves one Narration Language and one Voice for an entire Reading Session, allows an explicit user override, and does not switch automatically for mixed-language passages in v1 because mid-session switching would change provider configuration, buffering, cost, and speaker identity unpredictably.
